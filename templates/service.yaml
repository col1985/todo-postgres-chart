apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-postgres-chart.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "todo-postgres-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "todo-postgres-chart.selectorLabels" . | nindent 4 }}
